////import { Routes } from '@angular/router';
////export const routes: Routes = [];

import { MoviesComponent } from './pages/movies/movies.component';
export const routes: Routes = [
  { path: 'movies', component: MoviesComponent },
  // otras rutas ya agregadas
];
